apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: headlamp-values
    namespace: headlamp
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
