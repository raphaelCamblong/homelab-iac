apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-vars
  namespace: flux-system
data:
  DOMAIN: lab.raphlamenace.xyz
  K8S_API_HOST: 192.168.1.41
  GATEWAY_VIP: 192.168.1.200
  # WireGuard: LB IP is what the router port-forwards UDP 51820 to; WG_HOST is
  # what peers dial (public IP — swap for a DNS-only A record to survive ISP
  # IP rotation, see infrastructure/wireguard/README.md).
  WG_LB_IP: 192.168.1.201
  WG_HOST: 90.61.204.229
