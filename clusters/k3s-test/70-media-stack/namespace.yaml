apiVersion: v1
kind: Namespace
metadata:
  name: media
  labels:
    # baseline: all pods are linuxserver/jellyseerr containers running as
    # non-root (PUID/PGID 3000); no host* requirements.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/warn: baseline
