apiVersion: v1
kind: Service
metadata:
  name: qbit-nas
  namespace: media
spec:
  type: ClusterIP
  ports:
    # qBit Web UI is exposed via Gluetun's network namespace at host port 8080.
    - { name: http, port: 8080, targetPort: 8080, protocol: TCP }
