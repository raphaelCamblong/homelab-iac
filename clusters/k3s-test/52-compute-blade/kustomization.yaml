apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../homelab/infrastructure/compute-blade
patches:
  # See nodeselector-patch.yaml for the why.
  - path: nodeselector-patch.yaml
    target:
      kind: DaemonSet
      name: compute-blade-agent
